# Copy this file to .env and fill in your key:  cp .env.example .env
#
# Required. Get one at https://console.anthropic.com/settings/keys
# The server refuses to boot without it (fail fast beats failing mid-interview).
ANTHROPIC_API_KEY=

# Optional. Port to listen on. Render sets this automatically; default is 3000.
PORT=3000
